<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mattam.hacha">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 갤러리 접근 권한-->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="com.mattam.template.config.ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_main_launcher"
        android:roundIcon="@mipmap/ic_main_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final_project_11"
        android:usesCleartextTraffic="true">
<!--        android:networkSecurityConfig="@xml/network_security_config">-->
        <activity
            android:name="com.mattam.hacha.src.main.ReadyActivity"
            android:exported="false" />
        <activity
            android:name="com.mattam.hacha.src.main.LoginActivity"
            android:exported="true">
        </activity>
        <activity
            android:name="com.mattam.hacha.src.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.mattam.hacha.src.main.MainActivity"
            android:exported="true"></activity>

    </application>

</manifest>